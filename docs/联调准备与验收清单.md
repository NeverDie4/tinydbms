# 联调准备与验收清单

本文用于 compiler、core、storage 和 app 进入真实模块联调前的协作与验收，不替代
[消息契约详细设计](消息契约详细设计.md)、[模块交互契约](模块交互契约.md)和各阶段实现规格。
其中“已通过”必须有当前工作区的构建、测试或运行结果支持；未满足的项目保持为阻断项。

## 1. 当前状态

- compiler 已有可链接实现和独立 CTest，负整数、整数边界、表达式深度和大输入边界已有 hardening
  覆盖；第 3.1 节的 compiler 阻断项已全部关闭。
- core/app 已通过 fake compiler、fake storage 的行为测试，并通过真实 compiler + fake storage 的窄联调。
- storage 已提供真实实现；当前工作区在 `TINYDBMS_ENABLE_REAL_MODULES=ON` 下已用 g++ 完成构建和 51/51 CTest。
- `tinydbms.real_modules_integration` 已在临时目录自动覆盖
  `CREATE TABLE -> INSERT -> SELECT -> DELETE -> close -> reopen -> SELECT`，以及 UTF-8 数据目录、
  编译与语义错误位置、storage 运行期错误、批处理停止和 REPL 恢复。
- 上述结果说明已具备完整联调条件，不等同于最终验收；第 3 节的剩余门禁和异常边界仍需继续执行。

## 2. 当前可执行的真实模块联调

storage 已交付后，可以在隔离数据目录中执行完整模块联调；它不替代第 3.1 节的 compiler 契约硬化测试：

- 使用真实 `tinydbms_compiler`、真实 `tinydbms_core_objects`、真实 `tinydbms_storage`、真实 `CoreSession` 和 CLI runner；
- 不混入 fake compiler 或 fake storage；
- 至少验证 `CREATE TABLE → INSERT → SELECT → WHERE`、编译错误位置、Catalog 逐条更新和 CLI 退出码。

`tinydbms_compiler_core_cli_integration_test` 覆盖真实 compiler + fake storage 的窄联调；
`tinydbms.real_modules_integration` 覆盖真实 compiler/core/CoreSession/CLI/storage 和持久化链路。

## 3. 完整联调前阻断项

### 3.1 compiler

compiler 成员需要在联调前完成并补充回归测试；以下 5 项均已关闭：

1. 已覆盖：支持负整数文字，`INT32_MIN` 和 `INT32_MAX` 均有回归用例。
2. 已关闭：整数范围溢出返回 `CompileErrorKind::kSemantic`，词法形态错误仍为 `kLex`。
3. 已关闭：parser 内表达式复杂度预算 `256` 生效，超限返回 `kSyntax`，不生成超过 `256` 层的
   Plan；深层 `NOT`、深层 `AND` 均有拒绝用例，core 深度检查只作为防御性兜底。
4. 已关闭：整段 SQL 上限由 `kMaxSqlBytes` 固定为 1 MiB；`split_statements` 超限时不再抛出
   `std::length_error`，而是返回 `SplitStatementsResult` 中的 `kLex` CompileError。
5. 已覆盖：成功列表保持 `SplitStatement.start` 相对整段输入、`CompileError.location` 相对单条语句；
   分句普通失败不抛异常，core 直接返回 `kCompile` 且不使 Database 会话失效。

### 3.2 storage

Storage 真实实现已进入当前分支；联调阶段继续核对：

1. `open_storage`、`close_storage`、`list_tables`、`create_table`、`open_table`、`scan_next`、
   `close_cursor`、`insert` 和 `delete_records` 的公共签名与错误语义。
2. 非法生命周期调用、空删除请求、游标结束和重复关闭行为与契约一致。
3. 正常 close 后数据可重启读取；表元数据、TableId 和 RecordId 规则与 core 一致。
4. 公共 API 不让异常越过边界；部分写入/删除失败时返回可靠的部分成功数量。
5. storage 独立测试与真实 core/app 链路均需在当前工作区保留可重复的构建和测试证据。

## 4. 公共文件门禁

- 联调前比较 `include/tinydbms/` 与联调基线的差异。
- 任何公共头文件的字段、函数签名、错误语义或生命周期语义变化，必须先同步
  [消息契约详细设计](消息契约详细设计.md)，并记录变更原因、影响和验证结果。
- 需要通知依赖该头文件的 compiler、storage、core、app 成员；没有确认兼容性前，不把联调失败归因于实现细节。
- `src/compiler/*.hpp`、`src/core/*.hpp` 等模块内部头文件不属于公共 include，但仍需保持 CMake 目标的私有边界。

## 5. 联调构建顺序

所有 Linux 构建和验证使用 `g++`。建议使用任务专用的构建目录，避免覆盖已有构建产物：

```bash
BUILD_DIR=/tmp/tinydbms-integration-build

cmake -S . -B "$BUILD_DIR" -G Ninja \
  -DCMAKE_CXX_COMPILER=g++ \
  -DCMAKE_BUILD_TYPE=Debug \
  -DTINYDBMS_BUILD_TESTS=ON \
  -DTINYDBMS_ENABLE_REAL_MODULES=OFF
cmake --build "$BUILD_DIR" -j4
ctest --test-dir "$BUILD_DIR" --output-on-failure
```

先确认基线测试仍通过，再切换真实模块：

```bash
REAL_BUILD_DIR=/tmp/tinydbms-integration-build-real

cmake -S . -B "$REAL_BUILD_DIR" -G Ninja \
  -DCMAKE_CXX_COMPILER=g++ \
  -DCMAKE_BUILD_TYPE=Debug \
  -DTINYDBMS_BUILD_TESTS=ON \
  -DTINYDBMS_ENABLE_REAL_MODULES=ON
cmake --build "$REAL_BUILD_DIR" -j4
ctest --test-dir "$REAL_BUILD_DIR" --output-on-failure
```

真实模块配置下，构建失败必须先区分为接口/实现/链接问题；不能用 fake 测试通过代替真实链路验收。

## 6. 联调顺序

1. 各模块分别通过自己的单元测试和契约编译测试。
2. 以空的临时 `data_dir` 启动产品，验证 `open → list_tables → close`。
3. 依次验证 `CREATE TABLE → INSERT → SELECT → WHERE → DELETE → close → reopen → SELECT`。
4. 验证多语句脚本、编译错误位置、执行错误、REPL 继续读取和最终退出码。
5. 验证非法输入不会产生 storage 副作用，包括编译失败、非法 Plan、重复建表、类型错误和越界值。
6. 验证异常与部分成功路径：游标关闭、close_storage、部分 INSERT/DELETE、重启后 Catalog 和数据恢复。
7. 最后运行完整 CTest、`git diff --check` 和公共头文件差异检查。

## 7. 通过标准

真实联调完成必须同时满足：

- `TINYDBMS_ENABLE_REAL_MODULES=ON` 配置、构建和 CTest 全部通过；
- compiler、storage、core、app 的已有测试全部通过，且测试目标没有把 fake 与真实模块混用；
- 上述基本 SQL 生命周期和错误路径在临时数据目录中运行通过；
- close 后重启能够恢复表结构和已提交数据；
- compiler 的负数、整数边界、深表达式和大输入边界测试已覆盖；
- 真实 compiler/core/CoreSession/CLI/storage 链路有自动化 CTest 覆盖；
- 公共 `include/tinydbms/` 的变更已完成契约同步和成员通知；
- 工作区无调试产物、临时文件和未说明的差异。

存在未关闭的 Storage 阻断项，或真实模块运行验收尚未完成时，状态只能记为“已准备联调”，
不能记为“真实链路验收通过”。
